plugins {
    id 'com.gradle.enterprise' version '3.7.2'
}

gradleEnterprise {
    buildScan {
        termsOfServiceUrl = 'https://gradle.com/terms-of-service'
        termsOfServiceAgree = 'yes'
        publishAlways()
    }
}

rootProject.name = 'multi-project-hybrid'

include 'low-1', 'mid-1', 'top-1'